# Core dependencies
PyPDF2==3.0.1            # Extract text from PDF files
openai==1.30.1           # OpenAI API access for GPT-based LLMs



# LangChain ecosystem
langchain==0.2.1         # Framework for LLM workflows
faiss-cpu==1.7.4         # Vector similarity search engine
tiktoken==0.7.0          # OpenAI tokenizer



# (Optional) Environment handling
python-dotenv==1.0.1     # Load API key from .env files (if needed)
✅ .env (optional but recommended)
If you don't want to export your key every time, create a .env file:
